ssl_certificate /etc/letsencrypt/live/nsciwa.online/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/nsciwa.online/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    # Project root (optional if serving static files)
    root /var/www/html/NSCIWA;
    index index.html;

    location /static/ {
        alias /var/www/html/NSCIWA/static/;
    }

    location /media/ {
        alias /var/www/html/NSCIWA/media/;
    }

    location / {
        proxy_pass http://unix:/var/www/html/NSCIWA/gunicorn.sock;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
